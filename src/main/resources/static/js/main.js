// 일정이상 스크롤이 내려가면 클래스를 추가하거나 제거해주는 FUNCTION
/*$(function(){
	 var lastScroll = 0;
	 $(window).scroll(function(event){
		  var scroll = $(this).scrollTop();
		  if (scroll > 200){
		  //이벤트를 적용시킬 스크롤 높이
			   $(".header-middle").addClass("TEST");
		  }
		  else {
			   $(".header-middle").removeClass("TEST");
		  }
		  lastScroll = scroll;
	 });
});*/




